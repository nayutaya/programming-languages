version: "3"
services:
  shell:
    image: rust:1.41-buster
    volumes:
      - ".:/workspace"
    working_dir: /workspace
    user: "${USER_ID}:${GROUP_ID}"
    environment:
      - USER=${USER}
  grpc_server:
    build: grpc_server
